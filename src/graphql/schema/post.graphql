type Media {
  filename: String
  baseurl: String
}
input MediaInput {
  filename: String
  baseurl: String
}

input postInput {
  text: String
  Media: MediaInput
}

type Post {
  postId: String
  User: User
  userId: String
  text: String
  Media: Media
  createdDate: DateTime
  updateDate: DateTime
}
